---
controller_credentials:
  - name: "GIT-custom"
    credential_type: "GIT creds"
    organization: "Default"
    inputs:
      git_token: "{{ vaulted_controller_credentials_git_custom_git_token }}"
      git_username: maydin82

  - name: "varfile"
    credential_type: "Vault"
    organization: "Default"
    inputs:
      vault_password: "{{ vaulted_controller_credentials_varfile_vault_password }}"
  - name: "vmware"
    credential_type: "VMware vCenter"
    organization: "Default"
    inputs:
      host: vcsnsx-vc.infra.demo.redhat.com
      password: "{{ vaulted_controller_credentials_vmware_password }}"
      username: sandbox-9462c@demo
